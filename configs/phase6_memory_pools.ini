// Phase 6 Memory Pool Configuration

// Memory pool allocation settings
config->set("neuron_pool_size", size_t(1024), ConfigSource::Default);    // Default neuron pool size
config->set("synapse_pool_size", size_t(4096), ConfigSource::Default);   // Default synapse pool size
config->set("disable_memory_pools", false, ConfigSource::Default);        // Enable memory pools by default
config->set("memory_pool_cache_align", true, ConfigSource::Default);     // Enable cache alignment for performance