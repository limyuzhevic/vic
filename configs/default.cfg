# NLM Configuration File
# This is the default configuration for Phase 1 skeleton

# Random seed for reproducible experiments
random_seed = 42

# Simulation timestep in seconds
simulation_timestep = 0.001

# Number of neurons in the brain
neuron_count = 1000

# Number of brain regions
region_count = 2

# Connection probability for random connectivity
connection_probability = 0.1

# Plasticity settings
plasticity_learning_rate = 0.01
stdp_ltp_weight = 0.01
stdp_ltd_weight = 0.012
stdp_time_constant = 20.0

# Development settings
development_synaptogenesis_rate = 0.001
development_pruning_rate = 0.0001

# Neuromodulation settings
dopamine_baseline = 0.1

# Reward settings
reward_discount_factor = 0.99

# Environment settings
environment_name = GridWorld
environment_width = 10
environment_height = 10

# Logging settings
log_level = INFO
log_to_file = false
log_filename = nlm.log

# Simulation settings
max_simulation_steps = 10000
simulation_time_limit = 0.0

# Visualization settings
visualization_enabled = false
visualization_update_rate = 30.0

# Agent configuration
agent_enable_reward_modulation = true
agent_enable_structural_plasticity = true
agent_enable_development = true
agent_enable_curiosity = true
agent_curiosity_threshold = 0.3
agent_exploration_rate = 0.2
agent_novelty_decay = 0.99

# Memory system
agent_memory_working_memory_capacity = 1000
agent_memory_episodic_max_episodes = 100
agent_memory_associative_strength = 0.8

# Development parameters
agent_development_initial_plasticity = 1.0
agent_development_critical_period_end = 300.0
agent_development_maturation_end = 900.0
