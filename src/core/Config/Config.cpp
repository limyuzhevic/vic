Config improved with validation and better type safety

Implements a proper configuration system with:

1. Strong type checking and validation
2. Configuration metadata and documentation
3. Default values and presets
4. Validation rules and error handling
5. Better error reporting
6. Conversion utilities

Key improvements:

- Validates configuration values against type, range, and pattern constraints
- Provides rich metadata for configuration keys (description, type, defaults, validation rules)
- Supports default values and configuration presets
- Comprehensive error handling with detailed error messages
- Type-safe value access with automatic type checking
- Validation scoring and statistics
- Better debugging and diagnostic tools
- Configurable validation rules
- Batch operations for bulk configuration updates
- Configuration comparison and serialization utilities
- Conversion utilities for type-safe value conversion

This makes the configuration system much more robust, user-friendly, and maintainable while preserving all existing functionality.
